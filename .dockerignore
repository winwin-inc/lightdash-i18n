# 忽略本地依赖与缓存，避免 BuildKit 读取无效链接
**/node_modules
**/.pnpm-store
**/.turbo
**/.next
**/coverage
**/.cache

# 常见无关目录
.git
.idea
.vscode
.DS_Store

# CI 单 job 中前端构建产物必须进入 context，供 USE_PREBUILT_FRONTEND=1 使用（禁止排除）
!packages/frontend/build
!packages/frontend/build/**