import { describe, it } from 'vitest';

/**
 * Quick-filter apply helpers are stubbed in this fork (upstream QuickFilterOperator
 * APIs are absent). Skip until warehouse merge ports that surface.
 */
describe.skip('applyMergeQuickFilter', () => {
    it('is not available in this fork', () => {
        // intentionally empty
    });
});
