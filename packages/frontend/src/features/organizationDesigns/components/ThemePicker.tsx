// STUB: organization design ThemePicker not ported.
import { type FC } from 'react';

type Props = {
    compact?: boolean;
    value?: string | null;
    onChange?: (designUuid: string | null) => void;
};

export const ThemePicker: FC<Props> = () => null;
