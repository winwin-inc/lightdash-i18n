// STUB: Phase C — app Google Sheets sync not wired yet.
import { type FC } from 'react';

type Props = {
    projectUuid: string;
    appUuid: string;
    opened: boolean;
    onClose: () => void;
};

export const AppSyncModal: FC<Props> = () => null;
